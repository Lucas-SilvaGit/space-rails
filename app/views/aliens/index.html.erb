<p id="notice"><%= notice %></p>

<div class="container-fluid p-4">
  <div class="card shadow bg-white rounded">
    <div class="card-header d-flex bd-highlight">
      <div class="me-auto pt-2 pb-0 bd-highlight"><h6>Aliens</h6></div>
      <div class="pt-1 pb-0 bd-highlight">
        <%= link_to t("actions.new"),new_alien_path, class: "btn btn-info ps-4 pe-4 pt-1 pb-1 text-white"%>
      </div>
    </div>

    <div class="card-body px-5 my-3">
      <div class="d-flex bd-highlight">
        <table class= "table table-striped">
          <thead class="table-light">
            <tr>
              <th>Alien</th>
              <th>Age</th>
              <th>Planet</th>
              <th class="ps-5 pe-5 d-flex gap-1 justify-content-end"  colspan="3">Actions</th>
            </tr>
          </thead>

          <tbody>
            <% @aliens.each do |alien| %>
            <tr>
              <td><%= alien.name %></td>
              <td><%= alien.age %></td>
              <td><%= alien.planet.name %></td>            
              <td class="d-flex gap-2 justify-content-end">
                <%= link_to t("actions.delete"), alien, method: :delete, data: { confirm: t("actions.confirm")}, class: "btn btn-sm btn-danger px-3 py-0 rounded-3 w-20"%>
                <%= link_to t("actions.edit"), edit_alien_path(alien), class: "btn btn-sm btn-warning px-4 py-0 rounded-3 w-23"%>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <%= paginate @aliens %>
    </div>  
  </div>
</div>
